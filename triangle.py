def area(a, b, c):
    """Принимает числа a,b,c - возвращает полупериметр треугольника"""
    return (a + b + c) / 2


def perimeter(a, b, c):
    """Принимает числа a,b,c, - возвращает периметр треугольника"""
    return a + b + c
